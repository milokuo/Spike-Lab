# 《妖球排球》（暫名：SPIKE LAB）遊戲規則書 v0.2

> 文件目的：可落地的規則書。整合原始需求，補全不足處，並給出網頁技術選型與開發路線。
> 標記說明：〔原案〕＝需求原文轉寫；〔補全〕＝擴張或新增的設計；〔待驗證〕＝需原型實測才能定案的參數。
> v0.2 變更摘要：瞄準機制由「滑鼠游標投影」改為「方向鍵決定」；移除手把映射；技能系統由「12 個預設技能池」改為「每局隨機發放五塊積木、玩家自組六槽」；J／K／L 蓄力統一為「影響距離遠近」。

---

## 1. 一句話定位

**「手感紮實的排球對戰，加上會作妖的球」**——2 至 8 人連線派對遊戲，每個玩家用自己的主機遊玩，核心樂趣是「接舉殺三拍的 timing 手感」×「耐力／張力的資源博弈」×「積木自組技能的節目效果」。

### 1.1 設計支柱（所有設計決策的裁判）

1. **手感第一局就要好**：任何成長、技能、修飾都不得劣化基礎操控性。
2. **輸要比贏好笑**：壞球、漏接、體力耗盡的失敗演出，優先級等同得分演出。
3. **可讀性鐵律**：技能與妖球必須有預告破綻（前搖、變色、音效），被殺要心服口服。
4. **十五秒法則**：核心高潮（強力技能終結、絕地救球）必須能剪成 15 秒內的短影片。

---

## 2. 遊戲流程總覽

```
建房（房主）→ 分隊（2v2／3v3／4v4）→ 賽前 30 秒（查看站位、用當局五塊積木自組六槽技能）
→ 對戰（搶 15 分制，詳見 §9）→ 每 5 分觸發海克斯三選一（延伸階段，見 §11）
→ 賽末結算（MVP 回放、迷因時刻自動剪輯）→ 一鍵再戰
```

單場目標時長：**8～12 分鐘**。

---

## 3. 場地與視角

* 〔補全〕**3D 場地、低精度卡通渲染**（方塊人／膠囊人即可），攝影機為斜 45° 半俯視、跟隨球與己方半場，兩隊各自鏡像視角。
* 標準場地：長 18 單位 × 寬 9 單位，網高 2.43 單位（採現實比例便於調參）。
* 場地劃分為 **3×3 落點網格（每半場）**，用於落點品質計算（見 §6.3）。網格對玩家隱藏，但 debug 模式可顯示。
* 〔延伸〕隨機場地（傾斜、彈跳床、低重力等）列入 M4 之後，v0.1 不做。

---

## 4. 操作規格〔v0.2 改寫〕

| 輸入 | 功能 |
|---|---|
| WASD 或方向鍵 ↑↓←→ | 在球場上移動角色（八方向） |
| 空白鍵 | 跳躍（攔網、跳殺、跳發必需） |
| J | **接球（Dig）**：觸發即與球互動；同時按方向鍵決定意圖方向，按住即蓄力（見 §5、§6） |
| K | **舉球（Set）**：同上 |
| L | **擊球（Spike）**：同上 |
| U / I / O、M / , / . | 強力技能槽 ×6：每局進場前用當局隨機發到的五塊積木自組（見 §8） |
| Shift（按住） | **燃燒耐力**：站原地快速消耗耐力轉換為張力（見 §7.3） |
| Tab | 查看雙方技能配置（資訊透明，支撐心理戰） |

**九宮格心智模型**〔原案〕：`J K L`（基礎三模式）＋ `U I O`（積木技能上排）＋ `M , .`（積木技能下排），左手覆蓋一個 3×3 區域。

〔v0.2 移除〕原手把映射一節刪除；主控輸入以鍵鼠／鍵盤為準，手把支援延後評估。

---

## 5. 三模式的意義：方向意圖

〔v0.2 改寫〕J／K／L 是三種動作，各自決定球的**用途與意圖方向**。每個模式在觸球瞬間，會依「是否同時按方向鍵」決定球的意圖方向；蓄力則統一影響**距離遠近**。

### 5.1 三模式的方向與蓄力對照

| 模式 | 預設意圖方向（未按方向鍵） | 按方向鍵時 | 蓄力作用 |
|---|---|---|---|
| **J 接球** | 把球推向「目前處於 K（舉球）狀態、角度上最近的隊友」；若無人處於 K 狀態，則推向**最近的隊友** | （接球以隊友為目標，方向鍵不改變目標） | 決定「能不能把球推到目標隊友」——蓄力越足，越推得到位 |
| **K 舉球** | 把球垂直向上推高 | 往所按方向飛（含後方，例如同時按右後鍵則球往右後走） | 決定飛多遠 |
| **L 擊球** | 打向對方場地中央 | 按左（A／←）射對方**最左底線**；按右（D／→）射**最右底線** | 決定出力，反映為距離遠近 |

**觸球次數規則**（唯一定義處，§9 死球判定引用此處）：每隊過網前最多 3 次觸球，同一人不得連續觸球兩次（攔網觸球不計次）。

---

## 6. 觸球品質函數（核心手感）

### 6.1 總公式

```
quality = clamp01(
    f_distance(d)        // 球與角色的距離，§6.2
  × f_timing(Δt)         // 觸球時機（甜蜜窗口），§6.2
)

結果 quality ∈ [0, 1]，對映到球的三個輸出屬性：
  1. 落點／方向偏差（quality 越高，越貼近意圖方向；越低，偏差越大，見 §6.3）
  2. 高度合格度（quality 過低 → 高度不足，向下傳導至下一拍，見 §6.3.1）
  3. 出界／不過網機率（quality 極低 → 直接暴走出界或掛網丟分）

註：蓄力只決定「距離遠近」（見 §5.1、§6.2），耐力只影響「移動與行動能力」（見 §7），兩者皆不進入品質計算。
```

> 〔設計註記〕兩個內層子函數以**連乘**結合（懲罰嚴苛：任一項不佳都會顯著拉低品質），符合設計支柱「位置正確、時機正確缺一不可」的意圖。若 M0 實測發現連乘導致壞球過多、手感過硬，備案改為**加權平均**（較平滑）。此為 M0 第一優先調參項〔待驗證〕。

### 6.2 各子函數定義

**f_distance(d)** — 距離品質〔原案核心〕：

```
d = 球心與角色觸球點的水平距離
d ≤ 0.5 單位   → 1.0（甜蜜區）
0.5 < d ≤ 1.2  → 線性衰減 1.0 → 0.5（勉強夠到）
1.2 < d ≤ 1.8  → 0.5 → 0.1（魚躍撲救區：品質差但演出帥，觸發撲救動畫）
d > 1.8        → 無法觸球
```

**f_timing(Δt)** — 時機品質：

```
系統對每顆來球計算「理想觸球時刻 t*」（球進入角色可及範圍的最佳點）
Δt = |實際觸球時刻 − t*|
Δt ≤ 60ms          → 1.0，觸發「PERFECT」演出（閃光＋清脆音效＋0.1 秒微頓幀）
60ms < Δt ≤ 150ms  → 0.8（GOOD）
150ms < Δt ≤ 300ms → 0.5（OK）
Δt > 300ms         → 0.2（WHIFF 邊緣）
〔待驗證〕窗口毫秒數為第一優先調參對象；網路延遲補償見 §12。
```

**charge(c)** — 蓄力（只決定距離，不進品質）：

```
c ∈ [0,1]，按住觸球鍵每秒累積 0.8，上限 1.0（滿蓄約 1.25 秒）
距離加成 = 1 + 0.6 × c   （滿蓄 → 飛行距離 ×1.6）
三模式的蓄力語意（見 §5.1）皆為「影響距離遠近」：
  J → 能否把球推到目標隊友的距離
  K → 舉球飛多遠
  L → 擊球出力（射向底線的力道）
代價：蓄力期間移動速度 ×0.45（威力換機動）；蓄力持續消耗耐力（§7）
滿蓄後再持續按住 0.5 秒 → 過蓄（Overcharge），距離控制反而失準（飛過頭／出界風險上升），逼玩家抓放開時機
過蓄期耐力消耗維持 10／秒〔待驗證〕，避免站樁蓄力零成本
```

〔已移除〕舊版 stamina_penalty（低耐力品質懲罰）：耐力不再影響觸球品質，只影響移動與行動能力（見 §7）。

### 6.3 落點與方向解算（好球／壞球）〔v0.2 改寫〕

意圖方向不再由游標決定，改由「當前模式 ＋ 是否按方向鍵」決定（見 §5.1）：

```
1. 依 §5.1 決定意圖方向與目標（隊友／向上／對方中央或底線）
2. 實際結果 = 意圖 + 隨機偏差，偏差量 = base_scatter × (1 − quality)
   quality = 1.0 → 偏差 ≈ 0（指哪打哪 / 穩穩推到位）
   quality = 0.3 → 偏差可達 3 單位（球會亂飛）
3. 三模式的低品質後果各不相同：
   J（接球）低品質 → 接起的球偏向不利位置
   K（舉球）低品質 → 推起的高度不足
   L（擊球）低品質 → 球飛往不利位置／出界／不過網
4. 弧線生成：由觸球模式決定基準弧線（接=高慢、舉=高滯空、殺=平快），
   quality 過低時弧線最高點 × (0.5 + 0.5 × quality) → 高度不足
```

**優勢／不利位置定義**：對方半場 3×3 網格中，**無人覆蓋的格子**與**邊角格**為優勢格；有人覆蓋的格子為不利落點。蓄力值 c 會讓 L 擊球的偏差帶有朝最近優勢格的偏置（偏置強度 = 0.4 × c）——蓄力不只讓球飛更遠，還讓「運氣」偏向對你有利。

### 6.3.1 高度傳導鏈〔v0.2 新增，關鍵手感〕

高度不足會**向下傳導到下一拍**，這是本作接—舉—殺三拍相扣的核心：

```
K（舉球）品質不足 → 推起的球高度不足
 → L（擊球）者無法在最高點擊球，球在較低處被發射
 → 後果：容易觸網、被攔網、或球速不足
因此 L 的最終品質不僅取決於自身觸球，也取決於「來球高度」是否足夠。
一次糟糕的舉球會連鎖劣化下一拍的殺球——鼓勵玩家把三拍都打好。
```

### 6.4 球的軌跡為純函數〔為網路同步而立的鐵律〕

球一旦離手，其完整軌跡由**出手瞬間的參數包**（位置、速度向量、弧線類型、已掛載的積木效果、亂數種子）唯一決定，不依賴逐幀本地物理。伺服器只需廣播參數包，所有客戶端演算出完全一致的軌跡。

〔v0.2 補充，配合積木系統〕參數包除了初始軌跡，還可攜帶**延後生效的積木效果**：這類效果掛載於「該球未來某次被觸碰」的事件上，觸碰發生時才施加對應的加速度修飾（見 §8）。此機制仍為純函數——只要「未來效果」與其觸發條件都寫進參數包，雙端演算結果一致。**虛幻球（見 §8）純屬渲染層、不可觸、不進入判定**，因此不影響伺服器裁決的真實軌跡。

---

## 7. 耐力（Stamina）與張力（Tension）經濟

### 7.1 耐力

* 上限 100，回合開始滿值。
* 消耗：移動 2／秒、衝刺移動（雙擊方向）5／秒、跳躍 8／次、蓄力 10／秒、觸球 5／次、積木技能另計。
* 恢復：**每一球結束（死球）後 +25**；站立不動時 +4／秒。

### 7.2 低耐力狀態

* 耐力 < 30：移動速度 ×0.7，角色冒汗、喘氣音效（可讀性：對手看得見你累了——這本身是戰術資訊，對方會故意吊你）。
* 耐力 = 0：移動速度 ×0.5，無法蓄力、無法跳躍。角色動畫變成狼狽拖行——失敗演出即笑點。（移動變慢本身已是懲罰，觸球品質不再另外扣。）

### 7.3 張力

* 上限 100。**耐力每消耗 1 點，累積張力 0.6 點**（消耗即累積）。
* **燃燒（Shift 按住）**：每秒燒 15 耐力 → 得 15 張力（1:1 高效兌換，但站原地才有效），做出「我賭這一球要放大招」的宣言感——燃燒時角色發光，全場看得見，是可讀性也是節目效果。
* 張力達到技能需求值 → 對應技能槽亮起（HUD 與角色頭上同時顯示）。
* 張力**跨球保留、跨局清空**。

### 7.4 設計意圖

耐力是「持續作戰的油箱」，張力是「爆發的彈藥」，兩者由同一資源轉換——玩家永遠在「省油慢慢打」與「燒油放大招終結」之間做選擇。

---

## 8. 技能系統：積木自組〔v0.2 全面改寫〕

### 8.1 結構

* **J / K / L 永遠是基礎接／舉／殺**，不可改裝、無張力需求——保底手感。
* **U / I / O / M / , / . 六格為積木技能槽**。與舊版「12 個預設技能」不同，v0.2 採**積木自組**：
  * **每局進場時，玩家隨機拿到五塊積木。**
  * 玩家在賽前 30 秒把這五塊積木**自由分配到六個技能槽**。
  * 積木可留空槽，也可**多塊串接進同一槽**組成一段複合軌跡（串接順序＝軌跡先後）。
  * 積木的本質是**描述球的路徑軌跡**：施加在球上的加速度序列。
* 所有積木技能共通規則：
  * **張力費用 = 該槽所有積木的費用總和**：一個槽塞的積木越多、或塞進越強力的積木（狀態改變類 > 延後類 > 即時類，見 §8.2），發動所需張力就越高。這是核心權衡——強力複合技能發得少，輕量單積木技能發得勤。
  * 前搖預告：**0.3 秒**（角色變色＋專屬音效＋球出手後帶彩色軌跡殘影）——可讀性鐵律。積木越多、費用越高的槽，前搖與殘影越明顯，越好被對手讀到〔待驗證〕。
  * 部分積木另耗耐力（見 §8.2 各積木定義）。
  * 〔待驗證〕每回合每隊積木技能發動上限 **2 次**，防止同屏過多導致可讀性崩潰（原列於風險清單，v0.2 提升為正式規則）。

### 8.2 積木分類〔v0.2〕

**基本積木——即時軌跡類**（作用於當前這顆球的當下）

| 積木 | 效果 |
|---|---|
| 向前／後／左／右／上／下加速 | 給球該方向的加速度 x m/s |
| 順向加速 | 依球目前前進方向增加加速度 x m/s |
| 逆向減速 | 依球目前前進方向減少加速度 x m/s |

> 每塊積木的**作用時長是隨機的**，且**每個玩家拿到的數值不一樣**——同一種積木在不同人手上長短不同，構成每局獨特的技能組合。

**特殊積木——延後生效類**（作用於該球之後某次被觸碰時；可影響敵方）

| 積木 | 效果 |
|---|---|
| 下一次觸碰時施力 | 該球**下一次**被觸碰時，給予某方向加速度 x m/s，持續 n 秒 |
| 下二次觸碰時施力 | 同上，但作用於**下二次**觸碰 |

> 由於是「掛在未來觸碰事件」上，這類積木可干擾敵方接／舉／殺，是心理戰主力。實作見 §6.4 純函數約束。

**強力積木——狀態改變類**

| 積木 | 效果 |
|---|---|
| 變大／變小 | 球體積放大或縮小，直到之後第 N 次觸球為止 |
| 變重 | 球變重、更難被托高（此後每次獲得的向上加速度總是減少若干） |
| 虛幻分身 | 生成一顆虛幻球，持續 0.5 秒，其前進方向與真球不同——**純渲染、不可觸、不進入判定**（見 §6.4） |

〔待驗證〕各積木的加速度數值、隨機時長範圍、張力費用皆需實測調平；原則：**欺騙／延後類 > 狀態改變類 > 即時數值類**（越難反制越貴）。

### 8.3 平衡與保底

* 五塊積木隨機發放、時長人人不同——每局技能都不一樣，是本作的隨機樂趣來源，也是平衡風險。
* 〔待驗證〕是否需要保底機制（例如保證至少一塊即時類積木、避免抽到全是難用的延後類），留待 M3 實測決定。
* 〔延伸〕M4 之後可加「積木稀有度」「每週輪換積木池」等深度。

---

## 9. 計分與回合規則

* **每球得分制（Rally Point）**：任一方死球即得分，不論發球權。
* 一局 **15 分**，需領先 2 分，封頂 21。
* 發球：得分方下一球發球；發球為擊球模式觸球，可蓄力、可跳發（跳發的 PERFECT 時機窗口放寬 +10%、球速較快，但耐力消耗 ×2）。
* 死球判定：球落地、出界、觸網後落回本方、**觸球次數違規**（同隊觸球超過 3 次或同人連擊，定義見 §5 觸球次數規則）。
* **每球結束 → 全員耐力 +25、3 秒回位倒數**——這 3 秒是喘息、嘴砲與燃燒張力的窗口。
* 輪轉：每局開始隨機站位並顯示給玩家（賽前知道站位）；每 5 分全隊順時針輪轉一格，前排角色殺球品質 +10%、後排接球品質 +10%（站位＝軟性角色扮演，不硬性禁止行為）。

---

## 10. 反饋與演出規格（手感的另一半）

* PERFECT 觸球：0.1 秒微頓幀（hitstop）＋粒子＋高音「叮」。三拍全 PERFECT → 殺球慢動作 0.5 秒＋球體冒火——**這是本作的招牌可剪片段**。
* 壞球：角色觸球姿勢崩壞動畫（打到臉、空揮轉圈）、球音效變「噗嘰」。
* 出界丟分：鏡頭跟著球飛出去，全場「哦——」音效。
* 積木技能終結：得分後 1.5 秒 killcam 回放（自動存到賽末迷因集錦）。
* 賽末：自動剪輯本場 3 個最高潮片段（全 PERFECT、技能終結、魚躍救球），一鍵匯出短片／GIF——傳播管線內建。

---

## 11. 派對延伸層（M4 之後，先寫進規則書佔位）

* **海克斯**：每隊每得 5 分，全隊三選一抽取被動（例：全隊跳躍 +30%、對方場地邊線內縮 0.5 單位、每次觸球球隨機變大變小）。篩選標準：**截圖看不看得出來？看不出來的不收**。
* **隨機場地池**：傾斜場、彈跳床、低重力、桌面場。
* **自製球譜編輯器**（畫線＋觸發積木）：依賴 §6.4 純函數架構與 §8 積木系統，屆時為自然延伸。

---

## 12. 網路架構〔每人獨立主機——原案硬需求〕

### 12.1 拓撲

**權威伺服器（Authoritative Server）＋客戶端預測**：

* 房間制：一個 match ＝ 一個 server room。
* 玩家移動：客戶端預測＋伺服器校正（標準 reconciliation）。
* **觸球判定**：客戶端上報「觸球時刻＋模式＋蓄力值＋方向鍵狀態＋技能槽」，伺服器以**延遲補償**（回溯該玩家延遲時刻的球位置）驗證並裁決品質，廣播球的軌跡參數包（含延後生效積木）。
* **攔網／打手碰撞**：涉及逐幀碰撞的判定（如攔網手判定）同樣走伺服器權威＋延遲補償，避免「我看到打到手、你看到沒打到」。
* 球的飛行：所有端以參數包本地演算（§6.4），無需逐幀同步球位置 → 頻寬極省、軌跡零漂移。
* Tick rate：伺服器 30Hz，客戶端渲染插值 60fps。
* 〔待驗證〕timing 窗口（±60ms）與延遲補償的體感磨合是**全案最大技術風險**，M2 專項處理。

### 12.2 為什麼不做 P2P

8 人房 P2P 的 NAT 穿透與作弊防護成本高於一台小伺服器；且品質函數裁決需要單一權威，否則「我看到接到了、你看到沒接到」必然發生。

---

## 13. 技術選型（網頁開發）

### 13.1 建議棧（主方案）

| 層 | 選型 | 理由 |
|---|---|---|
| 語言 | **TypeScript** | 前後端共用型別與品質函數程式碼（isomorphic），伺服器裁決與客戶端預測跑同一份邏輯，杜絕雙端不一致 |
| 建置 | **Vite** | 秒級熱更新，迭代速度就是手感調校速度 |
| 渲染 | **Three.js** | 3D 低精度卡通風的標準答案；場景簡單（場地＋球＋≤8 個膠囊人），效能綽綽有餘 |
| 物理 | **自寫彈道學 ＋ Rapier（僅碰撞查詢）** | 球的軌跡是純函數拋物線＋積木修飾（§6.4、§8），不需要完整物理引擎模擬球；Rapier 只用於網／地面／攔網手的碰撞檢測。這個決定同時解決手感確定性與網路一致性 |
| 連線 | **Colyseus**（WebSocket）→ 效能不足時升級 **geckos.io**（WebRTC DataChannel，UDP 式） | Colyseus 房間制、狀態同步、TS 原生，與本案需求幾乎量身訂做；先用 TCP 驗證，若 timing 體感不行再換傳輸層（介面先抽象化） |
| 伺服器部署 | 任一便宜 VPS／Fly.io／Railway | 單台可撐數十房；爆紅再擴容 |
| 打包上架 | 後期以 **Tauri／Electron** 包殼上 Steam | 網頁版連結即玩負責傳播，Steam 版負責變現 |

### 13.2 備選與否決

* **Phaser／Pixi（2D）**：否決——排球的網前縱深與攔網是 3D 博弈，2D 會閹割戰術層。
* **Babylon.js**：可用，與 Three.js 同級；選 Three.js 純因生態與範例量更大。
* **Godot Web 匯出**：保留為 B 計畫——若網頁棧在 M2 驗證 timing 體感失敗，玩法邏輯（TS 寫的品質函數）翻譯成 GDScript 的成本可控。

---

## 14. 資料結構草案（節錄）〔v0.2 配合積木系統改寫〕

```typescript
// 前後端共用
interface TouchIntent {           // 客戶端 → 伺服器
  playerId: string;
  clientTime: number;             // 用於延遲補償
  mode: 'dig' | 'set' | 'spike';
  charge: number;                 // 0..1，影響距離
  dirInput: { x: -1|0|1; y: -1|0|1 };  // 觸球瞬間的方向鍵狀態（決定意圖方向，見 §5.1）
  skillSlot?: 'U'|'I'|'O'|'M'|','|'.';
}

interface BallLaunch {            // 伺服器 → 全體（純函數參數包）
  origin: Vec3;
  velocity: Vec3;
  arcType: 'dig' | 'set' | 'spike' | 'serve';
  quality: number;                // 伺服器裁決結果
  appliedBlocks?: BlockDef[];     // 本次施加的即時積木效果
  pendingBlocks?: PendingBlock[]; // 掛在未來觸碰事件上的延後積木（見 §6.4、§8.2）
  rngSeed: number;                // 虛幻球方向、隨機時長等的確定性來源
  serverTime: number;
}

interface BlockDef {              // 積木以資料驅動，玩家自組
  id: string;
  kind: 'instant' | 'delayed' | 'state';   // 即時／延後／狀態改變
  vector?: Vec3;                  // 施力方向（即時／延後類）
  magnitude?: number;             // 加速度大小 x m/s
  durationMs: number;             // 作用時長——每局隨機、人人不同
  tensionCost: number;
  staminaCost: number;
}

interface PendingBlock {          // 延後生效積木
  block: BlockDef;
  triggerOnTouch: 1 | 2;          // 該球下一次(1)或下二次(2)被觸碰時生效
}

interface SkillSlotConfig {       // 一個技能槽 = 一到多塊積木的有序組合
  slot: 'U'|'I'|'O'|'M'|','|'.';
  blocks: BlockDef[];             // 空陣列 = 該槽未配置
}
```

---

## 15. 開發里程碑

| 階段 | 內容 | 完成判準 |
|---|---|---|
| **M0（1～2 週）** | 單機手感原型：場地＋球＋單人＋J/K/L（方向鍵意圖＋蓄力）＋品質函數＋參數滑桿面板 | 自己連打 10 分鐘會不會想繼續 |
| **M1（+2 週）** | 本地雙人（同機雙輸入）＋耐力／張力＋計分回合 | 找一個朋友對打，對方笑了沒 |
| **M2（+3～4 週）** | Colyseus 上線、2 人跨網路對戰、延遲補償專項調校 | 50～100ms 延遲下 PERFECT 判定仍「感覺公平」 |
| **M3（+3 週）** | 積木系統（即時／延後／狀態三類）＋隨機發放五塊＋自組六槽介面＋前搖演出＋4v4 房間 | 陌生人房能自然打完整場並再戰 |
| **M4（+持續）** | 海克斯、隨機場地、迷因剪輯匯出、Steam 包殼 | 短片在社群有自然轉發 |

**鐵則**：每個 M 結束都問一次「下一階段哪些東西第一版可以不做」。

---

## 16. 初版數值表（M0 調參起點，全部〔待驗證〕）

| 參數 | 初值 |
|---|---|
| 角色移動速度 | 4.5 單位／秒（衝刺 6.5） |
| 跳躍高度／滯空 | 1.4 單位／0.7 秒 |
| PERFECT 窗口 | ±60ms |
| 蓄力累積速率／至滿 | 0.8／秒（約 1.25 秒滿蓄） |
| 蓄力期移動速度 | ×0.45 |
| 過蓄懲罰 | 滿蓄後續按 0.5 秒 → 距離控制失準（飛過頭／出界風險上升） |
| 殺球基準球速 | 14 單位／秒（滿蓄 ×1.6） |
| base_scatter | 3.0 單位 |
| 耐力上限／死球回復 | 100／+25 |
| 張力轉換率 | 消耗 1 耐力 → 0.6 張力（燃燒 1:1） |
| 每局發放積木數 | 5 塊（分配到 6 槽，可留空、可串接） |
| 每隊每回合技能發動上限 | 2 次 |

---

## 17. 風險清單

1. **延遲 vs timing 手感**（最高風險）：M2 若無法在 100ms 延遲下保住公平感，備案為放寬窗口＋加強客戶端預測演出（先響後判）。
2. **品質連乘過硬**：§6.1 連乘若導致壞球過多，備案改加權平均。M0 優先驗證。
3. **積木隨機不平衡**：五塊隨機積木可能出現「全是難用組合」，備案為保底規則或積木池分層（見 §8.3）。
4. **蓄力龜縮**：若人人站樁蓄力導致節奏死 → 過蓄懲罰加重、或蓄力上限與移動綁定。
5. **範疇膨脹**：§11 全部內容在 M3 驗證「核心好玩」之前一律凍結。
